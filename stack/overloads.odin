package stack


init :: proc {
    init_ds,
}

deinit :: proc {
    deinit_ds,
}

size :: proc {
    size_ss,
    size_ds,
}

capacity :: proc {
    capacity_ss,
}

is_empty :: proc {
    is_empty_ss,
    is_empty_ds,
}

is_full :: proc {
    is_full_ss,
}

pop :: proc {
    pop_ss,
    pop_ds,
}

push :: proc {
    push_ss,
    push_ds,
}

peek :: proc {
    peek_ss,
    peek_ds,
}

clear :: proc {
    clear_ss,
    clear_ds,
}
